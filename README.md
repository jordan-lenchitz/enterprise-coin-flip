# enterprise-grade coin flip

b2b SaaS-ready microservice that flips a coin using actually real quantum randomness.

## architecture
1. Run a Hadamard gate on a single qubit to achieve superposition.
2. Measure the qubit to collapse the wave function into a truly random `0` or `1`.

Time complexity: `O(1)`
Financial complexity: `O($$$)`
